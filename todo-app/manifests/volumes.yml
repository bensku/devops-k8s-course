apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: todo-cache
  namespace: $NAMESPACE
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi